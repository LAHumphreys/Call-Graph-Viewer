LINKED_LIBS= libExec libCallgrindData libProfData libSource\
             libUtils  \
			 libIOInterface \
			 libTest

BUILD_TIME_TESTS=source load path callCount stack profile graph
CPP_TAGS_FILE=profiler-tests-c++.tags
MODE=CPP

USE_BOOST=YES

NO_INTEL=YES

include ../makefile.include
